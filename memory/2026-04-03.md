# 2026-04-03

- Updated heartbeat guidance so task follow-up reviews should happen roughly every 4 hours when heartbeat polling is active.
- Added heartbeat preference state with `taskFollowupHours = 4`.
